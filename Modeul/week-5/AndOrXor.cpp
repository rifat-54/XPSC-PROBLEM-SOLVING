#include<bits/stdc++.h>
using namespace std;
int main()
{
   ios::sync_with_stdio(false);
   cin.tie(nullptr);
   
   int a,b;
  cin>>a>>b;
  
   cout<<"and=> "<<(a&b)<<'\n'; 
   cout<<"Or=> "<<(a|b)<<'\n'; 
   cout<<"Xor=> "<<(a^b)<<'\n'; 
     
     
    return 0;
}